{% extends 'layout.html.twig' %}

{% block content %}
    {% if state is not null and city is not null %}
        {{ 'forecast_%state%_%city%' | trans({'%state%': state, '%city%': city}) }}
    {% else %}
        {{'weather.description' | trans}}
    {% endif %}
{% endblock %}