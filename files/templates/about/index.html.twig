{% extends 'layout.html.twig' %}

{% block content %}
    {% if subject is not null %}
        {{ 'greeting_%subject%' | trans({'%subject%': subject}) }}
    {% else %}
        {{'about.description' | trans}}
    {% endif %}
{% endblock %}